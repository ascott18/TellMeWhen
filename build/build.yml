
trigger:
- master
- classic

pool:
  vmImage: 'ubuntu-latest'

steps:
- bash: sudo apt-get install -y pandoc
  displayName: "Install Packages"

- bash: sudo chmod +x ./build/create-lua-changelog && ./build/create-lua-changelog.sh
  displayName: "Build Changelog"

- bash: sudo chmod +x ./build/lib/packager/release.sh && ./build/lib/packager/release.sh
  displayName: "Package and Upload"

- task: PublishBuildArtifacts@1
  inputs:
    pathToPublish: .build/
    artifactName: build